{
  "name": "nightly",
  "version": "0.9.4",
  "description": "Karpathy-shape autoresearch loop that improves your Claude Code substrate (CLAUDE.md, hooks, skills, memory) overnight using your own real session history as the evaluation suite. One change per night, kept in git, measurably better each morning.",
  "author": {
    "name": "nightly contributors"
  },
  "components": {
    "agents": [
      "agents/nightly-optimizer.md"
    ],
    "commands": [
      "commands/nightly.md"
    ],
    "hooks": [
      {
        "event": "SessionStart",
        "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/nightly-surface.py\""
      }
    ]
  },
  "data_dir": "~/.claude/nightly",
  "src_dir": "${CLAUDE_PLUGIN_ROOT}/src"
}
